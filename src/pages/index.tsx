import styles from './index.less';
import Apis from '@/utils/apis';

export default function IndexPage() {
    // Apis.getBanner({hi:'1212'}).then((res: any) => {
    //   console.log(res)
    // }  )
    return <div>login</div>;
}
